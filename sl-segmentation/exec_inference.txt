# vai pra pasta do google research
# inicia o container
sudo docker run -it --runtime=nvidia --gpus all -v "$PWD":/google_research/ google-detection
cd google_research

# cria o tfrecord
python3 ./preprocessing/build_tfrecord.py --folder ./data/ --fps 30

# executa inferencia em video de teste
python3 inference.py --dataset_path ./data/<video-name>.tfrecord

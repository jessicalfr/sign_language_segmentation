#### Executando a inferência ####

# vai pra pasta do sl-segmentation
# inicia o container
sudo docker run -it --runtime=nvidia --gpus all -v "$PWD":/sl-segmentation/ sl-segmentation
cd sl-segmentation

# cria o tfrecord (por enquanto apenas para o formato default do openpose)
python3 ./preprocessing/build_tfrecord.py --skel /public_dgs_corpus/skel/ --type_skel DGS --fps 50 --output /sl-segmentation/data/test.tfrecord

# executa inferencia em video de teste
python3 inference.py --dataset_path ./data/test.tfrecord


#### Criando o tfrecord pra treino (DGS) ####

# vai pra pasta do sl-segmentation
# inicia o container
sudo docker run -it --runtime=nvidia --gpus all -v "$PWD":/sl-segmentation/ -v "/media/jessica/Windows-SSD/Users/jessi/OneDrive/Downloads - Linux/public_dgs_corpus/":/public_dgs_corpus/ sl-segmentation
cd sl-segmentation

# cria o tfrecord
python3 ./preprocessing/build_tfrecord_train.py --skel /public_dgs_corpus/skel/ --type_skel DGS --annot /public_dgs_corpus/annotations/ --fps 50 --output /sl-segmentation/data/dgs_corpus.tfrecord
